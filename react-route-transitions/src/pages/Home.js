import React from 'react';
import { Title } from '../components/Title';

const Home = () => {
  return (
    <div className="inner">
      <Title lineContent="This is the" lineContent2="Home page" />
      <div>
        <p className="info">
          Interaction children's also but her derive is at a an clearly,
          switching that haven't the then the even stitching down her of aged
          applications lots it there values
        </p>
      </div>
    </div>
  );
};

export default Home;
